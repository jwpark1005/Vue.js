export default {
    plus : function (a, b) {
        return a + b;
    },

    minus : function (a, b) {
        return a - b;
    },

    show : function (text) {
        console.log(text);
        return;
    },
}